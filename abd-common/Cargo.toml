[package]
name = "abd-common"
version = "0.1.0"
edition = "2021"

[features]
user = ["aya"]

[dependencies]
aya = { workspace = true, optional = true }
rkyv = { workspace = true, features = ["bytecheck"] }

[lib]
path = "src/lib.rs"
